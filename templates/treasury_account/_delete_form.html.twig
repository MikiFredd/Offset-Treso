<form method="post" action="{{ path('app_treasury_account_delete', {'id': treasury_account.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ treasury_account.id) }}">
    <button class="btn">Delete</button>
</form>
